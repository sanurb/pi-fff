---
description: Create a ringi review session for staged changes and open the web UI
---
Use the `/review` command to create a ringi review session and open it in a cmux browser pane. Stage files first with `git add`, then run `/review`. For branch reviews use `/review --branch main`. For commit reviews use `/review --commits <sha>`. Review in the ringi web UI — do not generate freeform review output.
